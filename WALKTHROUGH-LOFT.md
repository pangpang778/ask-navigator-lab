# loft 技能走查(LOGIC 叉:纯模块 + 壳 + 折叠)

场景:课程平台的真实状态机设计问题——"重修已完成课程时,路径进度怎么变?"

## 流程(按 SKILL.md 逐字执行)

1. **判定**:问题精确、散文聊不动(两条规则各自成立、互相冲突)→ loft,逻辑叉 ✓
2. **造形**:纯模块 `retake-core.mjs`(无 DOM/无 IO,可整体搬进真代码)+ 可点击壳(retake.html,四个场景按钮,每步完整状态可见),推 `loft/retake-progress` 分支
3. **船长反应**:点击 ② 重修 → **路径 100%→67% 回退、徽章却冻结**——两条规则的冲突在散文里不可见,实物一秒暴露。**第一形态被否**
4. **同会话修订 + 折叠**:模块改 best-attempt 语义,决策落 DECISION.md(船长原话 + 一行理由),推分支
5. **永不靠岸**:main 零改动;确认后的模块经 launch 作为真工作重进

## 结论

放样技能的三个关键判据全部生效:纯模块可搬、实物反应即决策、工件永不靠岸。冲突在花钱前暴露——这正是"先做出形,再开工"。

暴露的文本缺口见 FINDINGS-LOFT.md;反抄袭自查(与 Matt prototype 三文件的 8 词级 n-gram 比对)结果:**CLEAN,零重叠**。
